1. '\0' is the null termination character. It marks the end of the string.

2. index -1是什么意思?
eg LC058
...
int n = strlen(s);
int pos = n - 1;
while(s[pos] == ' ') {
   pos--;
}
if(pos == -1) return 0;
...

3.取substring:
str.substr(index, length);
